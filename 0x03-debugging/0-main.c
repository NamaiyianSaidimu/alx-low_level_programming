#include "main.h"
#include<stdio.h>

/**
 * main - our program's entry point.
 *
 * description: determining whether a number
 * is positive or negative or zero.
 *
 * Return: 0(Success!)
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
